// CASCADING

// Write an object with the attributes `x`, `add`, `multiply`, `divide`, and `subtract`. `x` should be the number `0`, and the others should be functions. The object should be able to chain calculations which directly get applied to x.

// Example:

// ```obj.add(5).multiply(2).subtract(2).divide(2).x === 4```